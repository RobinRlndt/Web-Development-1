const setup = () => {
    let global = {
        score : 0,
        players : [],
        PATH_PREFIX : "images\sprite",
        PATH_SUFFIX : ".png"
    }

    let score = global.score
}
window.addEventListener("load", setup);